apiVersion: v1
kind: Service

metadata:
 name: lex-chatbot-service

spec:
 type: LoadBalancer

 selector:
   app: lex-chatbot

 ports:
 - port: 80
   targetPort: 80
Deploy:
kubectl apply -f deployment.yaml

kubectl apply -f service.yaml
Check:
kubectl get svc
