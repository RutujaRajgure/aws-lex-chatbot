apiVersion: v1
kind: Service
metadata:
  name: lex-chatbot-service
spec:
  type: LoadBalancer
  selector:
    app: lex-chatbot
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
